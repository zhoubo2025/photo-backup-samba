name: Build Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Android SDK
        uses: android-actions/setup-android@v3

      - name: Gradle cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
          restore-keys: ${{ runner.os }}-gradle

      - name: Build Debug APK
        run: |
          ./gradlew assembleDebug --no-build-cache --no-configuration-cache

      - name: Upload Debug APK
        uses: actions/upload-artifact@v5
        with:
          name: photo-backup-debug
          path: app/build/outputs/apk/debug/app-debug.apk

  # Optional: Build Release APK with signing
  # Uncomment this job if you want to build release version
  # release:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up JDK 17
  #       uses: actions/setup-java@v4
  #       with:
  #         java-version: '17'
  #         distribution: 'temurin'
  #
  #     - name: Install Android SDK
  #       uses: android-actions/setup-android@v3
  #
  #     - name: Gradle cache
  #       uses: actions/cache@v4
  #       with:
  #         path: |
  #           ~/.gradle/caches
  #           ~/.gradle/wrapper
  #         key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
  #         restore-keys: ${{ runner.os }}-gradle
  #
  #     - name: Build Release APK
  #       run: |
  #         ./gradlew assembleRelease --no-build-cache --no-configuration-cache
  #
  #     - name: Upload Release APK
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: photo-backup-release
  #         path: app/build/outputs/apk/release/app-release.apk